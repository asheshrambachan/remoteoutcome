% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{cv_rsv}
\alias{cv_rsv}
\title{Cross-validation wrapper for RSV estimation}
\usage{
cv_rsv(fun, nfold = 5, ...)
}
\arguments{
\item{fun}{Function to apply. Must accept arguments with \code{_train} and
\code{_test} suffixes.}

\item{nfold}{Number of folds for cross-validation. Default is 5.}

\item{...}{Additional arguments passed to \code{fun}. Must include at least
a \code{D} argument to determine sample size.}
}
\value{
A list containing:
\describe{
  \item{coef_cv}{Cross-validated coefficient estimate}
  \item{out}{List of results from each fold}
}
}
\description{
Performs k-fold cross-validation for any estimation function that accepts
train and test data splits.
}
\examples{
\dontrun{
# Example with RSV estimator
cv_results <- cv_rsv(rsv_estimate, nfold = 5,
                     X = X, D = D, Y = Y, Se = Se, So = So)
}
}
