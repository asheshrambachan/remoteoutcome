% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{rsv_example}
\alias{rsv_example}
\title{Simulated RSV example dataset (binary outcome, K = 2)}
\format{
A data frame with 1,000 rows and 9 columns:
\describe{
\item{Y}{Integer (0/1). Binary outcome. \code{NA} for purely experimental
units (\code{S_e = 1, S_o = 0}).}
\item{D}{Integer (0/1). Treatment indicator. \code{NA} for purely
observational units (\code{S_e = 0, S_o = 1}).}
\item{S_e}{Integer (0/1). Experimental sample indicator.}
\item{S_o}{Integer (0/1). Observational sample indicator.}
\item{R1}{Numeric. Remotely sensed variable 1 (correlated with Y).}
\item{R2}{Numeric. Remotely sensed variable 2 (correlated with Y).}
\item{R3}{Numeric. Remotely sensed variable 3 (correlated with Y).}
\item{R4}{Numeric. Remotely sensed variable 4 (correlated with Y).}
\item{R5}{Numeric. Remotely sensed variable 5 (correlated with Y).}
}
}
\source{
Generated via \code{data-raw/generate_example_data.R}
}
\description{
A small synthetic dataset generated by \code{\link{sim_rsv_data}} to
illustrate and test the RSV estimator. It contains a purely experimental
sample, an overlap subsample, and a purely observational sample, with a
binary outcome and five remotely sensed variable (RSV) columns.
}
\details{
Generated with \code{sim_rsv_data(n_e = 300, n_o = 700, n_v = 100,
tau = 0.10, p0 = 0.30, n_r = 5, seed = 42)}. The true average treatment
effect is \eqn{\tau = 0.10}.

Unit types:
\itemize{
\item Pure experimental (\code{S_e = 1, S_o = 0}, 200 units):
\code{Y = NA}, \code{D} observed.
\item Overlap (\code{S_e = 1, S_o = 1}, 100 units):
both \code{Y} and \code{D} observed.
\item Pure observational (\code{S_e = 0, S_o = 1}, 700 units):
\code{Y} observed, \code{D = NA}.
}
The overlap units (\code{S_e = 1 & S_o = 1}) enable
\code{\link{test_stability}} and \code{\link{test_no_direct_effect}}.
}
\examples{
data(rsv_example)
head(rsv_example)
table(S_e = rsv_example$S_e, S_o = rsv_example$S_o)

}
