% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rsv_fitted.R
\name{rsv_fitted}
\alias{rsv_fitted}
\title{RSV Treatment Effect Estimator from Pre-fitted Predictions}
\usage{
rsv_fitted(
  Y,
  D,
  S_e,
  S_o,
  X = NULL,
  y_levels = c(0, 1),
  yK = y_levels[1],
  pred_Y,
  pred_D,
  pred_S_e,
  pred_S_o,
  theta_init = NULL,
  train.proportion = 0.2,
  sigma2.lower = NULL,
  sigma2.quantile = 0.01,
  seed = NULL
)
}
\arguments{
\item{Y}{Outcome variable (can be multi-category, \code{NA} where not observed).}

\item{D}{Treatment indicator (binary, \code{NA} where not observed).}

\item{S_e}{Experimental sample indicator (logical or 0/1).}

\item{S_o}{Observational sample indicator (logical or 0/1).}

\item{X}{Discrete covariates (matrix or data frame, optional). All columns will be used.}

\item{y_levels}{Levels of Y outcome (default c(0, 1) for binary).}

\item{yK}{Reference category for Y (default first level in y_levels).}

\item{pred_Y}{Matrix or data.frame with predicted probabilities for all Y levels.
Columns should be named pred_Y0, pred_Y1, pred_Y2, ... matching y_levels.
For binary Y, can also provide a vector of P(Y=1).}

\item{pred_D}{Predicted \eqn{P(D=1 | X, R, S_e=1)}.}

\item{pred_S_e}{Predicted \eqn{P(S_e=1 | X, R)}.}

\item{pred_S_o}{Predicted \eqn{P(S_o=1 | X, R)}.}

\item{theta_init}{Initial estimate of treatment effect (optional). If \code{NULL},
will be estimated from a subset of the data.}

\item{train.proportion}{Proportion of data to use for estimating \code{theta_init}
if it is not provided (default 0.2). Only used when \code{theta_init = NULL}.}

\item{sigma2.lower}{Lower bound for sigma-squared stabilization. If \code{NULL}
(default), uses quantile-based lower bound.}

\item{sigma2.quantile}{Quantile for sigma-squared lower bound (default 0.01).}

\item{seed}{Random seed for reproducibility when estimating \code{theta_init}
(default \code{NULL}).}
}
\value{
An object of class \code{"rsv"}. See \code{\link{rsv}} for details.
}
\description{
Estimates treatment effects using user-provided predicted probabilities.
Useful when you have already fitted prediction models externally or want
to use custom prediction methods.
}
\details{
This function allows users to provide their own predicted probabilities,
which can come from any source:
\itemize{
\item Models fitted using other packages (e.g., xgboost, keras)
\item Ensemble predictions
\item Pre-computed predictions from previous runs
\item Custom machine learning pipelines
}

If \code{theta_init} is not provided, the function randomly selects
\code{train.proportion} of the data to estimate it, then uses the remaining
data for the final RSV estimate.

Standard errors can be added using \code{\link{add_se}}.
}
\examples{
\dontrun{
# With theta_init provided
fit <- rsv_fitted(Y, D, S_e, S_o,
                  pred_Y = my_pred_Y,
                  pred_D = my_pred_D,
                  pred_S_e = my_pred_S_e,
                  pred_S_o = my_pred_S_o,
                  theta_init = 0.05)

# Without theta_init (will estimate from 20\% of data)
fit <- rsv_fitted(Y, D, S_e, S_o,
                  pred_Y = my_pred_Y,
                  pred_D = my_pred_D,
                  pred_S_e = my_pred_S_e,
                  pred_S_o = my_pred_S_o,
                  train.proportion = 0.2,
                  seed = 42)

# Add standard errors
fit <- add_se(fit, B = 1000, clusters = village_id, num.threads = 8)
}

}
\seealso{
\code{\link{rsv}}, \code{\link{rsv_split}}, \code{\link{cv.rsv}},
\code{\link{add_se}}
}
